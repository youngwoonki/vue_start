// import Vue from 'vue'
import {createApp} from 'vue' // 하나만 가져올때는
// import App from './App.vue'
import App from './App'

// Vue.createApp(App).mount('#app');
createApp(App).mount('#app'); // 이렇게 앞에 Vue.을 생략해도됨.